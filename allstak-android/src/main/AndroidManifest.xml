<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Connectivity breadcrumbs read the active network; outbound telemetry
         needs the internet permission. Both are normal (no runtime prompt). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application>
        <!-- Auto-init. The androidx.startup provider runs AllStakInitializer
             before Application.onCreate, reading the API key from manifest
             meta-data so a one-line install + key is all that is required.
             Apps that prefer explicit control add
             tools:node="remove" on the InitializationProvider entry and call
             AllStak.init(context) themselves. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="sa.allstak.android.AllStakInitializer"
                android:value="androidx.startup" />
        </provider>
    </application>
</manifest>
